'use strict';

describe('FormStore', function() {
  var store;

  beforeEach(function() {
    store = require('stores/FormStore.js');
  });

  it('should be defined', function() {
    expect(store).toBeDefined();
  });
});
