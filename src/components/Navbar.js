'use strict';

var React = require('react/addons');

require('styles/Navbar.scss');

var Navbar = React.createClass({
  render: function () {
    return (
        <div>
          <p>Content for Navbar</p>
        </div>
      );
  }
});

module.exports = Navbar; 

